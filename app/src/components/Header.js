import React from 'react';

import { StyledHeader } from '../styles/StyledHeader';

function Header() {
    return (
        <StyledHeader>
            <h1>Brad's To-Do app made without using create-react-app</h1>
        </StyledHeader>
    )
}

export default Header;